apiVersion: v1
kind: Service
metadata:
  name: {{ include "alerting-relay.fullname" . }}
  labels:
    {{- include "alerting-relay.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "alerting-relay.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
